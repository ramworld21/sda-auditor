import { p as proxyCustomElement, H, h } from './p-JCGAt0My.js';

const DgaList$1 = /*@__PURE__*/ proxyCustomElement(class DgaList extends H {
    constructor() {
        super();
        this.__registerHost();
        this.type = 'unordered';
        this.color = 'primary';
        //   @Prop() id: string = guid();
        this.title = "";
        this.items = [];
        this.renderIcon = (type) => {
            if (type === "with-icon") {
                return h("span", { class: "list-icon" }, "\u2713"); // Using checkmark icon for "with-icon" type
            }
            return null;
        };
    }
    render() {
        const Tag = this.type === 'ordered' ? 'ol' : 'ul';
        return (h("div", { key: '7d95361dcea178a59da32b4d08bf7f0560eb3d48', class: `list list--${this.type} list--${this.color}` }, this.title && h("h3", { key: '0344d56c05bb2edaee1716489128994fd076a0bf', class: "list-title" }, this.title), h(Tag, { key: 'd7e8b8e2a3f2162f9b0c6b25e9a6025d44f0ea71' }, this.items.map((item, index) => (h("li", { key: index, class: "list-item" }, this.type === "with-icon" && this.renderIcon(this.type), item))))));
    }
}, [0, "dga-list", {
        "type": [1],
        "color": [1],
        "title": [1],
        "items": [16]
    }]);
function defineCustomElement$1() {
    if (typeof customElements === "undefined") {
        return;
    }
    const components = ["dga-list"];
    components.forEach(tagName => { switch (tagName) {
        case "dga-list":
            if (!customElements.get(tagName)) {
                customElements.define(tagName, DgaList$1);
            }
            break;
    } });
}

const DgaList = DgaList$1;
const defineCustomElement = defineCustomElement$1;

export { DgaList, defineCustomElement };
